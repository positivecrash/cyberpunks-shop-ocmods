OpenCart 3.x — версия 1.1.0

Что делает
  Позволяет использовать partials в Twig-шаблонах OpenCart через {% include 'file.twig' %}.

Опциональный partial (файла может не быть)
  В Twig: {% include 'optional.twig' ignore missing %}

Сборка zip
  ./build-ocmod.sh

Установка: Расширения → Установка расширений → .ocmod.zip → Модификаторы → «Обновить».
