<?xml version="1.0" encoding="utf-8"?>
<modification>
	<name>Cyberpunks Shop Language Overrides</name>
	<code>cyberpunks_shop_language_overrides</code>
	<version>1.9.1</version>
	<author>positivecrash</author>
	<description>Minimal nl-nl core, URL locales, cb_lang theme strings (admin) with CSV import/export, shared SEO keywords, hreflang/og:locale, cart total labels, thousand separator, auto language pack stubs.</description>

	<!-- SEO decode: resolve keyword per current language -->
	<file path="catalog/controller/startup/seo_url.php">
		<operation error="skip">
			<search><![CDATA[				$query = $this->db->query("SELECT * FROM " . DB_PREFIX . "seo_url WHERE keyword = '" . $this->db->escape($part) . "' AND store_id = '" . (int)$this->config->get('config_store_id') . "'");]]></search>
			<add position="replace"><![CDATA[				$query = $this->db->query("SELECT * FROM " . DB_PREFIX . "seo_url WHERE keyword = '" . $this->db->escape($part) . "' AND store_id = '" . (int)$this->config->get('config_store_id') . "' AND language_id = '" . (int)$this->config->get('config_language_id') . "'");]]></add>
		</operation>
	</file>

	<!-- Admin: same SEO keyword allowed across languages -->
	<file path="admin/controller/catalog/product.php">
		<operation error="skip">
			<search><![CDATA[						if (count(array_keys($language, $keyword)) > 1) {]]></search>
			<add position="replace"><![CDATA[						if (false) {]]></add>
		</operation>
		<operation error="skip">
			<search><![CDATA[							if (($seo_url['store_id'] == $store_id) && (!isset($this->request->get['product_id']) || (($seo_url['query'] != 'product_id=' . $this->request->get['product_id'])))) {]]></search>
			<add position="replace"><![CDATA[							if (($seo_url['store_id'] == $store_id) && ((int)$seo_url['language_id'] == (int)$language_id) && (!isset($this->request->get['product_id']) || (($seo_url['query'] != 'product_id=' . $this->request->get['product_id'])))) {]]></add>
		</operation>
	</file>

	<file path="admin/controller/catalog/category.php">
		<operation error="skip">
			<search><![CDATA[						if (count(array_keys($language, $keyword)) > 1) {]]></search>
			<add position="replace"><![CDATA[						if (false) {]]></add>
		</operation>
		<operation error="skip">
			<search><![CDATA[							if (($seo_url['store_id'] == $store_id) && (!isset($this->request->get['category_id']) || ($seo_url['query'] != 'category_id=' . $this->request->get['category_id']))) {]]></search>
			<add position="replace"><![CDATA[							if (($seo_url['store_id'] == $store_id) && ((int)$seo_url['language_id'] == (int)$language_id) && (!isset($this->request->get['category_id']) || ($seo_url['query'] != 'category_id=' . $this->request->get['category_id']))) {]]></add>
		</operation>
	</file>

	<file path="admin/controller/catalog/information.php">
		<operation error="skip">
			<search><![CDATA[						if (count(array_keys($language, $keyword)) > 1) {]]></search>
			<add position="replace"><![CDATA[						if (false) {]]></add>
		</operation>
		<operation error="skip">
			<search><![CDATA[							if (($seo_url['store_id'] == $store_id) && (!isset($this->request->get['information_id']) || ($seo_url['query'] != 'information_id=' . $this->request->get['information_id']))) {]]></search>
			<add position="replace"><![CDATA[							if (($seo_url['store_id'] == $store_id) && ((int)$seo_url['language_id'] == (int)$language_id) && (!isset($this->request->get['information_id']) || ($seo_url['query'] != 'information_id=' . $this->request->get['information_id']))) {]]></add>
		</operation>
	</file>

	<file path="admin/controller/catalog/manufacturer.php">
		<operation error="skip">
			<search><![CDATA[						if (count(array_keys($language, $keyword)) > 1) {]]></search>
			<add position="replace"><![CDATA[						if (false) {]]></add>
		</operation>
		<operation error="skip">
			<search><![CDATA[							if (($seo_url['store_id'] == $store_id) && (!isset($this->request->get['manufacturer_id']) || (($seo_url['query'] != 'manufacturer_id=' . $this->request->get['manufacturer_id'])))) {]]></search>
			<add position="replace"><![CDATA[							if (($seo_url['store_id'] == $store_id) && ((int)$seo_url['language_id'] == (int)$language_id) && (!isset($this->request->get['manufacturer_id']) || (($seo_url['query'] != 'manufacturer_id=' . $this->request->get['manufacturer_id'])))) {]]></add>
		</operation>
	</file>

	<!-- Locale startup -->
	<file path="system/config/catalog.php">
		<operation error="skip">
			<search><![CDATA[	'startup/session',]]></search>
			<add position="after"><![CDATA[
	'extension/module/cyberpunks_locale/early',]]></add>
		</operation>
		<operation error="skip">
			<search><![CDATA[	'startup/seo_url']]></search>
			<add position="after"><![CDATA[,
	'extension/module/cyberpunks_locale/late']]></add>
		</operation>
	</file>

	<!-- cb_lang init + OC language-key overrides + thousand separator -->
	<file path="catalog/controller/startup/startup.php">
		<operation error="skip">
			<search><![CDATA[		$language->load($code);]]></search>
			<add position="after"><![CDATA[
		$cp_language_overrides_file = DIR_SYSTEM . 'library/cyberpunks_language_overrides.php';
		if (is_file($cp_language_overrides_file)) {
			require_once($cp_language_overrides_file);
			call_user_func(array('CyberpunksLanguageOverrides', 'apply'), $language, $code, $this->config);
		}
]]></add>
		</operation>
		<operation error="skip">
			<search><![CDATA[		$this->registry->set('language', $language);]]></search>
			<add position="after"><![CDATA[
		$cp_cb_lang_file = DIR_SYSTEM . 'library/cyberpunks_cb_lang.php';
		if (is_file($cp_cb_lang_file)) {
			require_once($cp_cb_lang_file);
			call_user_func(array('CyberpunksCbLang', 'init'), $this->registry);
		}
		$cp_thousand_point = $this->config->get('module_cyberpunks_language_overrides_thousand_point');
		$cp_thousand_map = array(
			'space' => ' ',
			'comma' => ',',
			'dot'   => '.',
			'nbsp'  => "\xc2\xa0",
			'none'  => ''
		);
		if (is_string($cp_thousand_point) && isset($cp_thousand_map[$cp_thousand_point])) {
			$language->set('thousand_point', $cp_thousand_map[$cp_thousand_point]);
		}
]]></add>
		</operation>
	</file>

	<file path="system/engine/loader.php">
		<operation error="skip">
			<search><![CDATA[			$output = $this->registry->get('language')->load($route, $key);]]></search>
			<add position="after"><![CDATA[
			if (!$key) {
				$cp_language_overrides_file = DIR_SYSTEM . 'library/cyberpunks_language_overrides.php';
				if (is_file($cp_language_overrides_file)) {
					require_once($cp_language_overrides_file);
					call_user_func(array('CyberpunksLanguageOverrides', 'apply'), $this->registry->get('language'), $route, $this->registry->get('config'));
				}
			}
]]></add>
		</operation>
	</file>

	<!-- Do not hard-exit on missing Twig templates (prevents white screens). -->
	<file path="system/library/template/twig.php">
		<operation error="skip">
			<search><![CDATA[exit();]]></search>
			<add position="replace"><![CDATA[return '';]]></add>
		</operation>
	</file>

	<!-- Register Twig function cb_lang -->
	<file path="system/library/template/twig.php">
		<operation error="skip">
			<search><![CDATA[			$twig = new \Twig\Environment($loader, $config);]]></search>
			<add position="after"><![CDATA[
			if (is_file(DIR_SYSTEM . 'library/cyberpunks_cb_lang.php')) {
				require_once(DIR_SYSTEM . 'library/cyberpunks_cb_lang.php');
				call_user_func(array('CyberpunksCbLang', 'registerTwig'), $twig);
			} else {
				// Library not on disk (half-finished OCMOD install): keep cb_lang defined so themes still render.
				$cp_cb_lang_passthrough = function ($source) {
					$args = func_get_args();
					array_shift($args);

					return $args ? vsprintf((string)$source, $args) : (string)$source;
				};

				if (class_exists('\\Twig\\TwigFunction')) {
					$twig->addFunction(new \Twig\TwigFunction('cb_lang', $cp_cb_lang_passthrough));
				} elseif (class_exists('Twig_SimpleFunction')) {
					$twig->addFunction(new \Twig_SimpleFunction('cb_lang', $cp_cb_lang_passthrough));
				}
			}
]]></add>
		</operation>
	</file>

	<!-- Header: hreflang + og:locale -->
	<file path="catalog/controller/common/header.php">
		<operation error="skip">
			<search><![CDATA[		$data['language'] = $this->load->controller('common/language');]]></search>
			<add position="before"><![CDATA[
		$data['hreflangs'] = array();
		$data['og_locale'] = '';
		$data['og_locale_alternates'] = array();

		$this->load->model('localisation/language');
		$cyberpunks_languages = $this->model_localisation_language->getLanguages();
		$cyberpunks_default_code = $this->config->get('config_language');

		$url_data = $this->request->get;
		unset($url_data['_route_']);
		unset($url_data['language']);

		if (!isset($url_data['route'])) {
			$cyberpunks_route = 'common/home';
		} else {
			$cyberpunks_route = $url_data['route'];
			unset($url_data['route']);
		}

		$cyberpunks_query = '';
		if ($url_data) {
			$cyberpunks_query = urldecode(http_build_query($url_data, '', '&'));
		}

		$cyberpunks_locale_map = function ($code) {
			$parts = explode('-', strtolower((string)$code));
			if (count($parts) >= 2) {
				return $parts[0] . '_' . strtoupper($parts[1]);
			}
			return str_replace('-', '_', (string)$code);
		};

		$cyberpunks_prev_language_id = $this->config->get('config_language_id');
		$cyberpunks_prev_language_code = isset($this->session->data['language']) ? $this->session->data['language'] : $cyberpunks_default_code;

		foreach ($cyberpunks_languages as $cyberpunks_language) {
			if (empty($cyberpunks_language['status'])) {
				continue;
			}

			$code = $cyberpunks_language['code'];
			$this->session->data['language'] = $code;
			$this->config->set('config_language_id', (int)$cyberpunks_language['language_id']);

			$href = $this->url->link($cyberpunks_route, $cyberpunks_query, true);

			$data['hreflangs'][] = array(
				'hreflang' => $code,
				'href'     => str_replace('&amp;', '&', $href)
			);

			$locale = $cyberpunks_locale_map($code);

			if ($code === $cyberpunks_prev_language_code) {
				$data['og_locale'] = $locale;
			} else {
				$data['og_locale_alternates'][] = $locale;
			}

			if ($code === $cyberpunks_default_code) {
				$data['hreflangs'][] = array(
					'hreflang' => 'x-default',
					'href'     => str_replace('&amp;', '&', $href)
				);
			}
		}

		$this->session->data['language'] = $cyberpunks_prev_language_code;
		$this->config->set('config_language_id', $cyberpunks_prev_language_id);

		if (!$data['og_locale'] && $cyberpunks_default_code) {
			$data['og_locale'] = $cyberpunks_locale_map($cyberpunks_default_code);
		}

]]></add>
		</operation>
	</file>

	<!-- Language switcher hrefs -->
	<file path="catalog/controller/common/language.php">
		<operation error="skip">
			<search><![CDATA[		$results = $this->model_localisation_language->getLanguages();]]></search>
			<add position="after"><![CDATA[
		if (is_file(DIR_SYSTEM . 'library/cyberpunks_language_switcher.php')) {
			require_once(DIR_SYSTEM . 'library/cyberpunks_language_switcher.php');
			$data['languages'] = CyberpunksLanguageSwitcher::build($this, $results);
			$results = array();
		}
]]></add>
		</operation>
	</file>

	<file path="catalog/controller/checkout/cart.php">
		<operation error="skip">
			<search><![CDATA[$data['totals'] = array();]]></search>
			<add position="after"><![CDATA[
			$cp_total_label_overrides = $this->config->get('module_cyberpunks_language_overrides_total_labels');
			if (!is_array($cp_total_label_overrides)) {
				$cp_total_label_overrides = array();
			}
			]]></add>
		</operation>

		<operation error="skip">
			<search><![CDATA[$data['totals'][] = array(]]></search>
			<add position="before"><![CDATA[
				$cp_total_code = isset($total['code']) ? (string)$total['code'] : '';
				$cp_total_title = $total['title'];
				if ($cp_total_code !== '' && !empty($cp_total_label_overrides[$cp_total_code])) {
					$cp_total_title = $cp_total_label_overrides[$cp_total_code];
				}
			]]></add>
		</operation>

		<operation error="skip">
			<search><![CDATA['title' => $total['title'],]]></search>
			<add position="replace"><![CDATA['title' => $cp_total_title,]]></add>
		</operation>
	</file>

	<file path="catalog/controller/checkout/payment_method.php">
		<operation error="skip">
			<search><![CDATA[$data['text_agree'] = sprintf($this->language->get('text_agree'), $this->url->link('information/information/agree', 'information_id=' . $this->config->get('config_checkout_id'), true), $information_info['title']);]]></search>
			<add position="replace"><![CDATA[
				$cp_text_agree_tpl = (string)$this->language->get('text_agree');
				$cp_text_agree_args = substr_count($cp_text_agree_tpl, '%s');
				$cp_text_agree_url = $this->url->link('information/information', 'information_id=' . $this->config->get('config_checkout_id'), true);

				if ($cp_text_agree_args <= 0) {
					$data['text_agree'] = $cp_text_agree_tpl;
				} elseif ($cp_text_agree_args === 1) {
					$cp_text_agree_link = '<a href="' . $cp_text_agree_url . '" class="agree" target="_blank" rel="noopener noreferrer"><b>' . $information_info['title'] . '</b></a>';
					$data['text_agree'] = sprintf($cp_text_agree_tpl, $cp_text_agree_link);
				} else {
					$data['text_agree'] = sprintf($cp_text_agree_tpl, $cp_text_agree_url, $information_info['title']);
				}
			]]></add>
		</operation>
	</file>

	<!-- Allow typing a new language code (stock UI only lists existing folders).
	     OCMOD matches one line at a time — use offset to replace the whole select block. -->
	<file path="admin/view/template/localisation/language_form.twig">
		<operation error="skip">
			<search><![CDATA[              <select name="code" id="input-code" class="form-control">]]></search>
			<add position="replace" offset="11"><![CDATA[              <input type="text" name="code" value="{{ code }}" placeholder="de-de" id="input-code" class="form-control" />
              <p class="help-block">Use a folder code like <code>de-de</code> or <code>fr-fr</code>. Missing catalog/admin language folders are created automatically from en-gb stubs.</p>
              {% if error_code %}
              <div class="text-danger">{{ error_code }}</div>
              {% endif %}]]></add>
		</operation>
	</file>

	<!-- Create language stubs before DB insert/update; validate code format. -->
	<file path="admin/controller/localisation/language.php">
		<operation error="skip">
			<search><![CDATA[			$this->error['code'] = $this->language->get('error_code');]]></search>
			<add position="replace" offset="1"><![CDATA[			$this->error['code'] = $this->language->get('error_code');
		} elseif (is_file(DIR_SYSTEM . 'library/cyberpunks_language_pack.php')) {
			require_once(DIR_SYSTEM . 'library/cyberpunks_language_pack.php');

			$cp_language_code = strtolower(trim((string)$this->request->post['code']));
			$this->request->post['code'] = $cp_language_code;

			if (!CyberpunksLanguagePack::isValidCode($cp_language_code)) {
				$this->error['code'] = 'Use a locale code like de-de or fr-fr.';
			} elseif ($cp_language_code !== 'en-gb' && $cp_language_code !== 'en' && !CyberpunksLanguagePack::ensureStub($cp_language_code)) {
				$this->error['warning'] = 'Could not create language folders under catalog/language and admin/language. Check write permissions.';
			}
		}]]></add>
		</operation>
	</file>

	<file path="admin/model/localisation/language.php">
		<operation error="skip">
			<search><![CDATA[	public function addLanguage($data) {]]></search>
			<add position="after"><![CDATA[
		if (is_file(DIR_SYSTEM . 'library/cyberpunks_language_pack.php')) {
			require_once(DIR_SYSTEM . 'library/cyberpunks_language_pack.php');
			CyberpunksLanguagePack::ensureStub(isset($data['code']) ? $data['code'] : '');
		}
]]></add>
		</operation>
		<operation error="skip">
			<search><![CDATA[	public function editLanguage($language_id, $data) {]]></search>
			<add position="after"><![CDATA[
		if (is_file(DIR_SYSTEM . 'library/cyberpunks_language_pack.php')) {
			require_once(DIR_SYSTEM . 'library/cyberpunks_language_pack.php');
			CyberpunksLanguagePack::ensureStub(isset($data['code']) ? $data['code'] : '');
		}
]]></add>
		</operation>
	</file>

	<!-- After language is saved to DB, copy missing product/information/route SEO keywords. -->
	<file path="admin/controller/localisation/language.php">
		<operation error="skip">
			<search><![CDATA[			$this->model_localisation_language->addLanguage($this->request->post);]]></search>
			<add position="after"><![CDATA[
			if (is_file(DIR_SYSTEM . 'library/cyberpunks_url_locale.php')) {
				require_once(DIR_SYSTEM . 'library/cyberpunks_url_locale.php');
				CyberpunksUrlLocale::ensureSeoUrls($this->db, (int)$this->config->get('config_language_id'));
			}
]]></add>
		</operation>
		<operation error="skip">
			<search><![CDATA[			$this->model_localisation_language->editLanguage($this->request->get['language_id'], $this->request->post);]]></search>
			<add position="after"><![CDATA[
			if (is_file(DIR_SYSTEM . 'library/cyberpunks_url_locale.php')) {
				require_once(DIR_SYSTEM . 'library/cyberpunks_url_locale.php');
				CyberpunksUrlLocale::ensureSeoUrls($this->db, (int)$this->config->get('config_language_id'));
			}
]]></add>
		</operation>
	</file>
</modification>
